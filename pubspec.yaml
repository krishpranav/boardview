name: boardview
description: A framework to build board view

version: 1.1.0

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
 
  cupertino_icons: ^1.0.2
  vs_scrollbar: ^0.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.boardview
    iosBundleIdentifier: com.example.boardview
